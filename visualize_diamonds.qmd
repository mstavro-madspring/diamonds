---
title: "Diamonds Viz"
format: html
date: "`r Sys.time()`"
---

# Diamonds Deployment Example
```{r, include=FALSE}
pak::pak("ggplot2")

library(ggplot2)
```

### A debatably useful visualization, small change
```{r}
ggplot(diamonds, aes(x = carat, y = depth, color = cut)) + geom_point() + scale_fill_viridis_d() + theme_minimal()
```